version: '3.9'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - ./.env.docker
    ports:
      - '8000:8000'
    depends_on:
      - db
  db:
    image: postgres:latest
    env_file:
      - ./.env.docker
    ports:
      - '5433:5433'

